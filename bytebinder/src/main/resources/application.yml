spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5000/ByteBinderDB
    username: eunbi
    password: qwer1234
  jpa:
    database: postgresql
#    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
        show_sql: true
    hibernate:
      ddl-auto: create-drop